{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "preDeployCommand": "sh -c 'alembic upgrade head && alembic current --check-heads && python scripts/bootstrap_database_roles.py --reconcile-runtime-grants'",
    "startCommand": "sh -c 'exec sleep infinity'",
    "restartPolicyType": "NEVER"
  }
}
